# Project Pipeline

The full updated pipeline is documented in `pipeline_modified_local_qwen.md`.

This project is structured as:

- `frontend/` for the React + Vite user interface.
- `backend/` for the Express API that talks to Ollama.
- `pipeline_modified_local_qwen.md` for the detailed architecture and build plan.
